"""
ER (Entity-Relationship) model validation and render-plan utilities.

This package is intentionally standalone (no DB, no persistence) and provides:
- Input schemas matching the frontend ERModel JSON contract
- Validation rules with structured feedback
- A simple Chen-diagram render plan generator (no image generation)
"""

